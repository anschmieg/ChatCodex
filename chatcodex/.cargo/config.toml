[build]
jobs = 4
rustc-wrapper = "sccache"
target-dir = "/home/t3/.cache/codex-rs/chatcodex-target"

# Use LLD from the rust toolchain — BFD ld is ~5x slower on aarch64
[target.aarch64-unknown-linux-gnu]
linker = "/home/t3/.local/bin/ld.lld"
