//! Module re-export so other crates can use `well_known::*` paths.

pub use crate::discovery::jwks;
pub use crate::discovery::oauth_authorization_server;
pub use crate::discovery::oauth_protected_resource;
