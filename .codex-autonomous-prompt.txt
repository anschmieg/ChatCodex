Implement the approved persistent-project/autonomous-run architecture for ChatCodex completely on the current feature branch.

Constraints:
- Obey AGENTS.md. ChatGPT remains the only LLM; deterministic backend only; no provider SDKs, model calls, or hidden agent loop.
- Work only in this clone/branch. Do not push/deploy. Do not modify workflow or Dockerfile.
- Use strict TDD: focused failing tests first and observed RED, minimal implementation, then GREEN/full verification. Rust 1.95.
- Preserve existing public tools/client compatibility where reasonable.

Deliverables:
1. Persistent projects with stable IDs for existing repos, registered workspaces, and scratch projects. Replace process-global active workspace with selected run/project state. Persist atomically beneath workspace base, isolated by CHATCODEX_CLIENT_ID; never persist credentials.
2. Persistent runs with project, objective, acceptance criteria, inspect/plan/execute/verify phase, active/paused/blocked/awaiting_approval/completed/cancelled status, plan/checklist, checkpoints, autonomy envelope (turn/runtime/step limits, allowed local actions/commits), timestamps, and continuation lease/counter data. Survive restart and be resumable/list/get-able.
3. Compact deterministic lifecycle tools for project create/select/list/get and run start/list/get/update/resume/cancel. Validate transitions/limits server-side. Existing setup_workspace/update_plan/todo and all workspace tools operate on active run when selected, with legacy fallback.
4. Add authoritative run metadata to every coding tool result when active: run_id, phase/status, work_remaining, next_action, limits/lease state. Keep catalog output schemas accurate.
5. Add standards-compliant ChatGPT App MCP resource/tool metadata and a self-contained HTML/JS run-status component. When and only when work remains, it obtains a duplicate-safe continuation lease/nonce via deterministic tool then calls window.openai.sendFollowUpMessage with only the run ID. Include delay, unique nonce, expiry, max turns/runtime/steps and safe pause. Never continue terminal/paused/blocked/awaiting_approval states. Missing bridge/component leaves run resumable.
6. Strengthen server instructions: create/select a run from first coding request, chain fine-grained tools while work remains, verify acceptance criteria, finish only at terminal state.
7. Do not weaken git/network/safety policy. External effects require awaiting_approval unless an existing authorized mechanism explicitly permits them.
8. Update AGENTS.md and relevant architecture/tool docs; remove stale single-active-run/process-local state claims.
9. Tests must cover restart persistence across two harness/store instances, invalid transition rejection, lease duplicate safety/expiry/limits, result metadata, widget/resource presence, schema parity, and legacy tools.
10. Run fmt/check and mcp-server unit/integration tests. Use clear modules rather than bloating lib.rs. Atomic JSON is preferred over a heavy DB if locking, corruption handling, namespacing, and tests are sound. No placeholders/TODO stubs. Make clean commits only after verification.

Toolchain quirks:
- Use `rustup run 1.95.0 cargo ...`.
- The tracked .cargo config may reference t3 paths; use environment overrides or a temporary uncommitted local adjustment, never commit machine-specific paths.
- Do not trust a compile alone: exercise the actual lifecycle/resource behavior through tests.

Finish with exact files, commit(s), tests, RED evidence, and any genuine blocker.